#ifndef MAINWINDOW_H
#define MAINWINDOW_H

#include <QMainWindow>

class MainWindow : public QMainWindow
{
    Q_OBJECT

private:
    Q_DISABLE_COPY(MainWindow)

protected:

public:
    MainWindow(QWidget* parent = nullptr);

signals:

public slots:

};

#endif // MAINWINDOW_H