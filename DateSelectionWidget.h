#ifndef DATESELECTIONWIDGET_H
#define DATESELECTIONWIDGET_H

#include <QWidget>

class DateSelectionWidget : public QWidget
{
    Q_OBJECT

private:
    Q_DISABLE_COPY(DateSelectionWidget)

protected:

public:
    DateSelectionWidget(QWidget* parent = nullptr);

signals:

public slots:

};

#endif // DATESELECTIONWIDGET_H