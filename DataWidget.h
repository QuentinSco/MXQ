#ifndef DATAWIDGET_H
#define DATAWIDGET_H

#include <QWidget>

class DataWidget : public QWidget
{
    Q_OBJECT

private:
    Q_DISABLE_COPY(DataWidget)

protected:

public:
    DataWidget(QWidget* parent = nullptr);

signals:

public slots:

};

#endif // DATAWIDGET_H